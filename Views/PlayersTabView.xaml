<UserControl x:Class="Padel.Manager.Views.PlayersTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Padel.Manager.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="900">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,10"
                Padding="10"
                BorderBrush="#D8DDE8"
                BorderThickness="1"
                CornerRadius="6">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="220" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,20,0">
                    <TextBlock Text="Nom du joueur" Margin="0,0,0,4" FontWeight="SemiBold" />
                    <TextBox x:Name="NewPlayerNameBox" Height="Auto" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0,0,20,0">
                    <TextBlock Text="Niveau" FontWeight="SemiBold" />
                    <ComboBox x:Name="NewPlayerLevelBox" Height="Auto" SelectedIndex="0">
                        <ComboBoxItem Content="1" />
                        <ComboBoxItem Content="2" />
                        <ComboBoxItem Content="3" />
                        <ComboBoxItem Content="4" />
                        <ComboBoxItem Content="5" />
                    </ComboBox>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Ajouter un joueur"
                        Margin="0,0,8,0"
                        Padding="15,0"
                        Click="AddPlayerButton_OnClick" />

                <Button Grid.Column="4"
                        Content="Enregistrer les niveaux"
                        Margin="0,0,8,0"
                        Padding="15,0"
                        Click="SaveLevelsButton_OnClick" />

                <Button Grid.Column="5"
                        Content="Recharger"
                        Padding="15,0"
                        Click="ReloadPlayersButton_OnClick" />
            </Grid>
        </Border>

        <local:PlayerGridControl Grid.Row="1" />
    </Grid>
</UserControl>
